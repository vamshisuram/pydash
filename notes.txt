
python -m venv .venv
. .venv/bin/activate
pip install Flask

pip install <package> --upgrade && pip freeze > requirements.txt
pip install flask-mysql --upgrade && pip freeze > requirements.txt
pip install mysql-connector-python --upgrade && pip freeze > requirements.txt

mysql -u username -p
source /path/to/load_script.sql


pip install flask --upgrade && pip freeze > requirements.txt
pip install Flask-SQLAlchemy --upgrade && pip freeze > requirements.txt


pip install streamlit requests pandas --upgrade && pip freeze > requirements.txt